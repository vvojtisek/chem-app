import type { BreadcrumbItem } from "./page-header";

/** First breadcrumb of every exercise: the practice hub, also reachable from the main navigation. */
export const PRACTICE_BREADCRUMB: BreadcrumbItem = { label: "Procvičovat", href: "/procvicovani" };
